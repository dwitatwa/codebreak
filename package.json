{
  "name": "codebreak-monorepo",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@11.23.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "view": "pnpm --filter @codebreak/viewer dev"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
